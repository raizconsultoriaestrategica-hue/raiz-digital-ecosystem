// Re-exporta o client oficial gerado pela integração Supabase do Lovable.
// Mantido para compatibilidade com imports antigos (@/lib/supabase).
export { supabase } from "@/integrations/supabase/client";
export const isSupabaseConfigured = true;
